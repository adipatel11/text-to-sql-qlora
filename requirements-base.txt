# Eval, data prep, plotting. CPU-only friendly; runs on the eval/orchestration
# machine. (Execution accuracy itself only needs the stdlib sqlite3.)
datasets>=2.18
requests>=2.31
pyyaml>=6.0
tqdm>=4.66
matplotlib>=3.8
numpy>=1.26
